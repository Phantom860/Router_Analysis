# 已修改

## 爬虫数量

增加爬取数量，已在代码中进行修改，便于进行可视化操作

```python
# clawer.py
max-comment = 200  # 原来为40
product-count = 10  # 原来为10
```

## 文件保存

修改爬虫数量参数后重新进行爬取和清洗操作，将原来的数据分别移动到`router_raw_data_1.csv`和`router_analysis_data_1.csv`

# 待修改

## 爬虫顺序

### 修改策略

- **改变分组遍历顺序**：将最有价值的评价类型提前，确保优先采集差评、追评和中评。
- **保留全局上限**：避免单个商品爬取时间过长，但保证在有价值分组中尽力收集。

### 调整后的分组顺序

```py
# 修改为
REVIEW_GROUPS = [
    ("差评", "1"),      # 最有价值，优先采集，直接暴露产品缺陷（断流、掉线、发热）
    ("追评", "null"),   # 使用后追评，次优先，反映长期使用后的真实体验
    ("中评", "3"),      # 中立意见，客观优缺点并存
    ("好评", "5"),      # 正面评价，信息量通常较低，多为简单好评
    ("全部", "null"),   # 剩余补充，混合展示，作为补充兜底
]

# 原代码
REVIEW_GROUPS = [
    ("全部", "null"),
    ("好评", "5"),
    ("中评", "3"),
    ("差评", "1"),
    ("追评", "null"),
]
```



## 爬取规则

目前存在问题：同一链接下存在多个型号，爬取默认型号（real提供的链接中，默认都是AX3000）

### 1. 核心主力：AX3000（抓取权重最高）

电信家庭千兆宽带、小微商铺、全屋 Mesh 组网**标准集采档位**，占采购量 70% 以上

- 无线速率：2.4G 574Mbps + 5G 2402Mbps，合计 3000Mbps，满血 2×2 160MHz WiFi6
- 硬性集采门槛：全千兆网口、支持**小翼管家、EasyMesh 电信标准组网、WPA3、OFDMA**
- 电信定制标杆型号（爬虫必收录）：
  1. TP-Link WTA301（电信专供）
  2. 中兴 ZXHN E1630（市面最走量电信版）
  3. 中兴E1630电信版
  4. 天邑 TY-6201A Pro（电信网关配套原厂）
  5. H3C RC3000 电信定制版
  6. 小米 / Redmi AX3000 电信合约版

### 2. 入门走量：AX1800（第二优先级，低端宽带 / 老房置换）

适配 300M–500M 宽带、预算极低批量装机，存量替换量大

- 速率：2.4G 574M + 5G 1201M
- 电信定制型号：TP XDR1850 电信版、天邑 TY1801、中兴 E1600

### 3. 中高端升级：AX5400（第三优先级，大户型 / 2000M 宽带 / 政企小微）

大平层、多设备、2000M 宽带、门店多终端并发场景，高毛利机型

- 速率：2.4G 574M + 5G 4804M（4×4 MIMO）
- 电信集采型号：TP XDR5410 电信版、中兴 AX5400 Pro、天邑 AX5400



## 爬虫星级

没有2星

```python
# 原代码
def infer_star(comment: str, fallback: str = "null") -> str:
    if any(word in comment for word in ["差评", "失望", "不好", "掉线", "断网", "不满意", "很差"]):
        return "1"
    if any(word in comment for word in ["一般", "还行", "中评", "普通"]):
        return "3"
    if fallback != "null":
        return fallback
    if any(word in comment for word in ["不错", "满意", "好用", "稳定", "很快"]):
        return "5"
    return "4"
```



## 数据清洗

### 1.添加路由器领域自定义词典

在分词前加载自定义词库，确保专有名词不被切碎。

###  2.改进关键词匹配逻辑

基于分词结果进行匹配，而非在原始文本中硬匹配子串。

### 3. 丰富停用词表

移除 `len(word) > 1` 限制，保留单字评价词（如“快”、“稳”、“卡”等），并完善停用词表 `stop_words`